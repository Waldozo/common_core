#include "phonebook.hpp"
#include "contact.hpp"

int main()
{
    std::string command;

    PhoneBook iPhone;

    std::cout << "enter : ADD or SEARCH or EXIT";
    std::getline (std::cin,command);
    if(command == "ADD");
    {

    }
    return(0);
}